{
  "name": "ando-zara-wedding",
  "version": "1.0.0",
  "description": "Wedding invitation for Andranik & Zaruhi",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=ando-zara-wedding",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=ando-zara-wedding --branch=preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "vite": "^5.4.0",
    "wrangler": "^4.110.0"
  }
}
